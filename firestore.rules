rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Backend-only access - all operations go through the server
    // These rules are for the emulator; production uses service account
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

